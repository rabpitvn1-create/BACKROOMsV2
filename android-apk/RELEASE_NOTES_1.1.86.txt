Backroom 1.1.86

- Thay hệ Story Interaction A/B/C bằng Story Decision v2.
- Story Compiler chỉ khóa canon intent và decision anchor; không sinh ba đáp án build-time.
- Runtime prefetch hai nhánh phụ trong lúc người chơi đọc, nên khi bấm lựa chọn không còn gọi AI và không lộ latency.
- UI lựa chọn cốt truyện chỉ hiển thị ba dòng dạng •, không A/B/C và không đánh dấu canon/trap.
- Thêm hidden outcomes CANON_PROGRESS, CONVERGE và TRAP_LOOP; trap quay về decision anchor mà không báo “chọn sai”.
- Hidden outcome map chỉ tồn tại trong Android Core; WebView/localStorage chỉ nhận choiceId + text.
- ChoiceId dùng token UUID ngẫu nhiên, không suy ngược được provenance từ contextHash, decisionId hay vị trí.
- Provider order thống nhất: Gemini key 1→5 trước, Haiku là fallback cuối.
- Generated Story Compiler schema lên v2 và giữ manuscript là canonical source.
